// main.js
import { add, subtract } from './math-utils.js';

const number = add(10, 5);          
const number1 = subtract(20, 7);

// Log the results to the console
console.log(number);
console.log(number1);